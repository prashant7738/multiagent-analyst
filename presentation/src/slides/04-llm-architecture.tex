% ============================================
% LLM ARCHITECTURE
% ============================================

\begin{frame}{LLM Architecture -- Llama Transformer}
    \begin{itemize}
        \item The model used in the pipeline is Llama, a decoder-only transformer language model.
        \item It processes the input context through repeated self-attention and feed-forward blocks.
        \item The attention layers help the model relate each token to earlier tokens in the prompt.
        \item This architecture makes it suitable for semantic tagging, schema inference, and structured reasoning.
    \end{itemize}
\end{frame}

\begin{frame}[plain]
    \begin{center}
        \includegraphics[width=\textwidth,height=0.85\textheight,keepaspectratio]{src/images/llm_transformer_architecture.png}
    \end{center}
\end{frame}