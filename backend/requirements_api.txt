# API layer dependencies (in addition to the existing pipeline requirements).
# The pipeline itself already depends on: groq, google-genai, langchain-core,
# langgraph, pandas, numpy, scipy, matplotlib, jinja2, weasyprint,
# scikit-learn, python-dotenv (see pyproject.toml).

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
python-dotenv>=1.0.0
psycopg[binary]>=3.3.4
passlib[bcrypt]>=1.7.4
cryptography>=42.0.0
